name: 'Premier test'
on: [pull_request]

jobs:  
  test:
    runs-on: ubuntu-latest

    steps:
    - run: npm install
    - run: npm run dev
    - run: npm run build
    - run: npm run lint
    - run: npm run test
